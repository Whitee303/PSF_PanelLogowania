            // const input = document.getElementById("login");
            // const password = document.getElementById("password");

            // function aha() {
            //     Loggin_Sec.remove()
            // }

            // function getCharset(login, x) {
            //     if (password.value.length < 4) {
            //         console.error("Standard error: Length must be greater than four")
            //         return;
            //     } else {
            //         login = input.value;
            //         x = password.value;
            //         console.log(`Password count:${password.value.length}`);
            //         alt.emit('someEvent', login, x)
            //     }
            // }